const { sequelize } = require('./src/config/database'); (async () => { try { await sequelize.query('ALTER TABLE "CustomerRequests" ALTER COLUMN "status" TYPE VARCHAR(255) USING "status"::text;'); await sequelize.query('DROP TYPE IF EXISTS "enum_CustomerRequests_status" CASCADE;'); console.log('Fixed status column!'); process.exit(0); } catch (err) { console.error(err); process.exit(1); } })();
